IGEL_HP_T640_EMMC_FIX
	  Fix reboot hangs on HP t640 devices.

	  Source: IGEL-downgraded
	  version: 5.16
	  Commit: 4be33cf187036744b4ed84824e7157cfc09c6f4c
	  URL: https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/tree/drivers/mmc/host/sdhci-pci-o2micro.c?h=linux-5.16.y
diff --git a/drivers/mmc/host/sdhci-pci-o2micro.c b/drivers/mmc/host/sdhci-pci-o2micro.c
index 058bef1c7e41..ad34c73f5040 100644
--- a/drivers/mmc/host/sdhci-pci-o2micro.c
@@ -314,13 +314,17 @@ static int sdhci_o2_dll_recovery(struct sdhci_host *host)
 static int sdhci_o2_execute_tuning(struct mmc_host *mmc, u32 opcode)
 {
 	struct sdhci_host *host = mmc_priv(mmc);
+#ifndef CONFIG_IGEL_HP_T640_EMMC_FIX
 	struct sdhci_pci_slot *slot = sdhci_priv(host);
 	struct sdhci_pci_chip *chip = slot->chip;
+#endif
 	int current_bus_width = 0;
 	u32 scratch32 = 0;
 	u16 scratch = 0;
+#ifndef CONFIG_IGEL_HP_T640_EMMC_FIX
 	u8  scratch_8 = 0;
 	u32 reg_val;
+#endif
 
 	/*
 	 * This handler implements the hardware tuning that is specific to
diff --git a/drivers/mmc/host/sdhci-pci-o2micro.c b/drivers/mmc/host/sdhci-pci-o2micro.c
index 058bef1c7e41..ad34c73f5040 100644
--- a/drivers/mmc/host/sdhci-pci-o2micro.c
@@ -339,6 +343,7 @@ static int sdhci_o2_execute_tuning(struct mmc_host *mmc, u32 opcode)
 	scratch |= O2_SD_PWR_FORCE_L0;
 	sdhci_writew(host, scratch, O2_SD_MISC_CTRL);
 
+#ifndef CONFIG_IGEL_HP_T640_EMMC_FIX
 	/* Update output phase */
 	switch (chip->pdev->device) {
 	case PCI_DEVICE_ID_O2_SDS0:
diff --git a/drivers/mmc/host/sdhci-pci-o2micro.c b/drivers/mmc/host/sdhci-pci-o2micro.c
index 058bef1c7e41..ad34c73f5040 100644
--- a/drivers/mmc/host/sdhci-pci-o2micro.c
@@ -607,6 +613,7 @@ static void sdhci_pci_o2_set_clock(struct sdhci_host *host, unsigned int clock)
 		dmdn_200m = 0x25100000;
 	}
 
+#ifndef CONFIG_IGEL_HP_T640_EMMC_FIX
 	if ((host->timing == MMC_TIMING_UHS_SDR104) && (clock == 200000000)) {
 		pci_read_config_dword(chip->pdev, O2_SD_PLL_SETTING, &scratch_32);
 
