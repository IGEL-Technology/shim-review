IGEL_KYOCERA_USB_SET_INTF_QUIRK
	  Without this quirk set the printer did not print anything.

	  Source: IGEL
diff --git a/drivers/usb/core/quirks.c b/drivers/usb/core/quirks.c
index 21f9b7fec8cb..4631fc20defc 100644
--- a/drivers/usb/core/quirks.c
@@ -522,6 +532,12 @@ static const struct usb_device_id usb_quirk_list[] = {
 	{ USB_DEVICE(0x1908, 0x1315), .driver_info =
 			USB_QUIRK_HONOR_BNUMINTERFACES },
 
+#ifdef CONFIG_IGEL_KYOCERA_USB_SET_INTF_QUIRK
+	/* lang@igel: Kyocera FS-1350DN printer
+	 * doesn't print anything if quirk is not set */
+	{ USB_DEVICE(0x0482, 0x0392), .driver_info = USB_QUIRK_NO_SET_INTF },
+#endif
+
 	/* Protocol and OTG Electrical Test Device */
 	{ USB_DEVICE(0x1a0a, 0x0200), .driver_info =
 			USB_QUIRK_LINEAR_UFRAME_INTR_BINTERVAL },
