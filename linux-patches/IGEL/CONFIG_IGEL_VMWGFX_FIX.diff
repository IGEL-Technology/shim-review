IGEL_VMWGFX_FIX
	  There are some problems it sys-simplefb is in use with pci
          request which fails. So make sure simplefb is not use in
          vmwgfx cases. Also try to work around a QEMU issue as there
          the vmwgfx will fail after init and leaves you without any
          graphic output.

	  Source: IGEL
diff --git a/drivers/firmware/sysfb.c b/drivers/firmware/sysfb.c
index 889e5b05c739..c376ab44ef2e 100644
--- a/drivers/firmware/sysfb.c
@@ -29,17 +29,24 @@
 #include <linux/init.h>
 #include <linux/kernel.h>
 #include <linux/mm.h>
+#ifndef CONFIG_IGEL_NO_SIMPLEFB_PREVENT_VM_SEGFAULT
 #include <linux/pci.h>
+#endif
 #include <linux/platform_data/simplefb.h>
 #include <linux/platform_device.h>
 #include <linux/screen_info.h>
 #include <linux/sysfb.h>
+#ifdef CONFIG_IGEL_VMWGFX_FIX
+#include <linux/pci.h>
+#endif
 
 static struct platform_device *pd;
 static DEFINE_MUTEX(disable_lock);
 static bool disabled;
 
+#ifndef CONFIG_IGEL_NO_SIMPLEFB_PREVENT_VM_SEGFAULT
 static struct device *sysfb_parent_dev(const struct screen_info *si);
+#endif
 
 static bool sysfb_unregister(void)
 {
diff --git a/drivers/firmware/sysfb.c b/drivers/firmware/sysfb.c
index 889e5b05c739..c376ab44ef2e 100644
--- a/drivers/firmware/sysfb.c
@@ -138,7 +157,77 @@ static struct device *sysfb_parent_dev(const struct screen_info *si)
 
 	return NULL;
 }
+#endif /* ifndef CONFIG_IGEL_NO_SIMPLEFB_PREVENT_VM_SEGFAULT */
+
+#ifdef CONFIG_IGEL_NO_SIMPLEFB_PREVENT_VM_SEGFAULT
+static __init int sysfb_init(void)
+{
+	struct screen_info *si = &screen_info;
+	struct simplefb_platform_data mode;
+	const char *name;
+	bool compatible;
+	int ret = 0;
+
+	mutex_lock(&disable_lock);
+	if (disabled)
+		goto unlock_mutex;
+
+	sysfb_apply_efi_quirks();
+
+	/* try to create a simple-framebuffer device */
+# ifdef CONFIG_IGEL_VMWGFX_FIX
+	if (pci_get_device(0x15ad, 0x0405, NULL) ||
+	    pci_get_device(0x15ad, 0x0406, NULL)) {
+		compatible = 0;
+	} else {
+		compatible = sysfb_parse_mode(si, &mode);
+	}
+# else
+	compatible = sysfb_parse_mode(si, &mode);
+# endif
+
+	if (compatible) {
+		pd = sysfb_create_simplefb(si, &mode);
+		if (!IS_ERR(pd))
+			goto unlock_mutex;
+	}
+
+	/* if the FB is incompatible, create a legacy framebuffer device */
+	if (si->orig_video_isVGA == VIDEO_TYPE_EFI)
+		name = "efi-framebuffer";
+	else if (si->orig_video_isVGA == VIDEO_TYPE_VLFB)
+		name = "vesa-framebuffer";
+	else if (si->orig_video_isVGA == VIDEO_TYPE_VGAC)
+		name = "vga-framebuffer";
+	else if (si->orig_video_isVGA == VIDEO_TYPE_EGAC)
+		name = "ega-framebuffer";
+	else
+		name = "platform-framebuffer";
+
+	pd = platform_device_alloc(name, 0);
+	if (!pd) {
+		ret = -ENOMEM;
+		goto unlock_mutex;
+	}
+
+	sysfb_set_efifb_fwnode(pd);
 
+	ret = platform_device_add_data(pd, si, sizeof(*si));
+	if (ret)
+		goto err;
+
+	ret = platform_device_add(pd);
+	if (ret)
+		goto err;
+
+	goto unlock_mutex;
+err:
+	platform_device_put(pd);
+unlock_mutex:
+	mutex_unlock(&disable_lock);
+	return ret;
+}
+#else /* ifndef  CONFIG_IGEL_NO_SIMPLEFB_PREVENT_VM_SEGFAULT */
 static __init int sysfb_init(void)
 {
 	struct screen_info *si = &screen_info;
diff --git a/drivers/firmware/sysfb.c b/drivers/firmware/sysfb.c
index 889e5b05c739..c376ab44ef2e 100644
--- a/drivers/firmware/sysfb.c
@@ -164,7 +253,17 @@ static __init int sysfb_init(void)
 	}
 
 	/* try to create a simple-framebuffer device */
+# ifdef CONFIG_IGEL_VMWGFX_FIX
+	if (pci_get_device(0x15ad, 0x0405, NULL) ||
+	    pci_get_device(0x15ad, 0x0406, NULL)) {
+		compatible = 0;
+	} else {
+		compatible = sysfb_parse_mode(si, &mode);
+	}
+# else
 	compatible = sysfb_parse_mode(si, &mode);
+# endif
+
 	if (compatible) {
 		pd = sysfb_create_simplefb(si, &mode, parent);
 		if (!IS_ERR(pd))
diff --git a/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c b/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c
index 8ff958d119be..95630bffcd96 100644
--- a/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c
@@ -258,6 +258,10 @@ static int vmw_assume_16bpp;
 static int vmw_probe(struct pci_dev *, const struct pci_device_id *);
 static int vmwgfx_pm_notifier(struct notifier_block *nb, unsigned long val,
 			      void *ptr);
+#ifdef CONFIG_IGEL_VMWGFX_FIX
+static int vmw_setup_pci_resources(struct vmw_private *dev,
+				   u32 pci_id);
+#endif
 
 MODULE_PARM_DESC(restrict_iommu, "Try to limit IOMMU usage for TTM pages");
 module_param_named(restrict_iommu, vmw_restrict_iommu, int, 0600);
diff --git a/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c b/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c
index 8ff958d119be..95630bffcd96 100644
--- a/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c
@@ -703,6 +707,7 @@ static void vmw_vram_manager_fini(struct vmw_private *dev_priv)
 	ttm_range_man_fini(&dev_priv->bdev, TTM_PL_VRAM);
 }
 
+#ifndef CONFIG_IGEL_VMWGFX_FIX
 static int vmw_setup_pci_resources(struct vmw_private *dev,
 				   u32 pci_id)
 {
diff --git a/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c b/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c
index 8ff958d119be..95630bffcd96 100644
--- a/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c
@@ -773,6 +778,7 @@ static int vmw_setup_pci_resources(struct vmw_private *dev,
 
 	return 0;
 }
+#endif /* ! CONFIG_IGEL_VMWGFX_FIX */
 
 static int vmw_detect_version(struct vmw_private *dev)
 {
diff --git a/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c b/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c
index 8ff958d119be..95630bffcd96 100644
--- a/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c
@@ -835,6 +841,16 @@ static int vmw_driver_load(struct vmw_private *dev_priv, u32 pci_id)
 	int ret;
 	enum vmw_res_type i;
 	bool refuse_dma = false;
+#ifdef CONFIG_IGEL_VMWGFX_FIX
+	uint32_t capabilities = 0;
+
+	capabilities = vmw_read(dev_priv, SVGA_REG_CAPABILITIES);
+	if (!(capabilities & SVGA_CAP_DISPLAY_TOPOLOGY) &&
+	    !(capabilities & SVGA_CAP_PITCHLOCK) &&
+	    !vmw_fifo_have_pitchlock(dev_priv)) {
+		return -ENOSYS;
+	}
+#endif
 
 	vmw_sw_context_init(dev_priv);
 
diff --git a/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c b/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c
index 8ff958d119be..95630bffcd96 100644
--- a/drivers/gpu/drm/vmwgfx/vmwgfx_drv.c
@@ -1617,14 +1633,97 @@ static struct pci_driver vmw_pci_driver = {
 	}
 };
 
+#ifdef CONFIG_IGEL_VMWGFX_FIX
+static int vmw_setup_pci_resources(struct vmw_private *dev,
+				   u32 pci_id)
+{
+	resource_size_t rmmio_start;
+	resource_size_t rmmio_size;
+	resource_size_t fifo_start;
+	resource_size_t fifo_size;
+	int ret;
+	struct pci_dev *pdev = to_pci_dev(dev->drm.dev);
+
+	pci_set_master(pdev);
+
+	/* IGEL ensure we tried to release the conflicting framebuffer */
+	ret = aperture_remove_conflicting_pci_devices(pdev, driver.name);
+
+	ret = pci_request_regions(pdev, "vmwgfx probe");
+	if (ret) {
+		drm_warn(&dev->drm, "PCI request regions failed continue anyways.");
+	}
+
+	dev->pci_id = pci_id;
+	if (pci_id == VMWGFX_PCI_ID_SVGA3) {
+		rmmio_start = pci_resource_start(pdev, 0);
+		rmmio_size = pci_resource_len(pdev, 0);
+		dev->vram_start = pci_resource_start(pdev, 2);
+		dev->vram_size = pci_resource_len(pdev, 2);
+
+		drm_info(&dev->drm,
+			"Register MMIO at 0x%pa size is %llu kiB\n",
+			 &rmmio_start, (uint64_t)rmmio_size / 1024);
+		dev->rmmio = devm_ioremap(dev->drm.dev,
+					  rmmio_start,
+					  rmmio_size);
+		if (!dev->rmmio) {
+			drm_err(&dev->drm,
+				"Failed mapping registers mmio memory.\n");
+			pci_release_regions(pdev);
+			return -ENOMEM;
+		}
+	} else if (pci_id == VMWGFX_PCI_ID_SVGA2) {
+		dev->io_start = pci_resource_start(pdev, 0);
+		dev->vram_start = pci_resource_start(pdev, 1);
+		dev->vram_size = pci_resource_len(pdev, 1);
+		fifo_start = pci_resource_start(pdev, 2);
+		fifo_size = pci_resource_len(pdev, 2);
+
+		drm_info(&dev->drm,
+			 "FIFO at %pa size is %llu kiB\n",
+			 &fifo_start, (uint64_t)fifo_size / 1024);
+		dev->fifo_mem = devm_memremap(dev->drm.dev,
+					      fifo_start,
+					      fifo_size,
+					      MEMREMAP_WB);
+
+		if (IS_ERR(dev->fifo_mem)) {
+			drm_err(&dev->drm,
+				  "Failed mapping FIFO memory.\n");
+			pci_release_regions(pdev);
+			return PTR_ERR(dev->fifo_mem);
+		}
+	} else {
+		pci_release_regions(pdev);
+		return -EINVAL;
+	}
+
+	/*
+	 * This is approximate size of the vram, the exact size will only
+	 * be known after we read SVGA_REG_VRAM_SIZE. The PCI resource
+	 * size will be equal to or bigger than the size reported by
+	 * SVGA_REG_VRAM_SIZE.
+	 */
+	drm_info(&dev->drm,
+		 "VRAM at %pa size is %llu kiB\n",
+		 &dev->vram_start, (uint64_t)dev->vram_size / 1024);
+
+	return 0;
+}
+#endif /* CONFIG_IGEL_VMWGFX_FIX */
+
+
 static int vmw_probe(struct pci_dev *pdev, const struct pci_device_id *ent)
 {
 	struct vmw_private *vmw;
 	int ret;
 
+#ifndef CONFIG_IGEL_VMWGFX_FIX
 	ret = aperture_remove_conflicting_pci_devices(pdev, driver.name);
 	if (ret)
 		goto out_error;
+#endif
 
 	ret = pcim_enable_device(pdev);
 	if (ret)
