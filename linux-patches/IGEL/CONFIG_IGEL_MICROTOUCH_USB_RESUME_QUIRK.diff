IGEL_MICROTOUCH_USB_RESUME_QUIRK
	  Enable USB reset/resume quirk for MicroTouch Systems touchscreens.

	  Source: IGEL
diff --git a/drivers/usb/core/quirks.c b/drivers/usb/core/quirks.c
index 21f9b7fec8cb..4631fc20defc 100644
--- a/drivers/usb/core/quirks.c
@@ -332,6 +337,11 @@ static const struct usb_device_id usb_quirk_list[] = {
 	/* Alcor Micro Corp. Hub */
 	{ USB_DEVICE(0x058f, 0x9254), .driver_info = USB_QUIRK_RESET_RESUME },
 
+#ifdef CONFIG_IGEL_MICROTOUCH_USB_RESUME_QUIRK
+	/* MicroTouch Systems touchscreen */
+	{ USB_DEVICE(0x0596, 0x051e), .driver_info = USB_QUIRK_RESET_RESUME },
+#endif
+
 	/* appletouch */
 	{ USB_DEVICE(0x05ac, 0x021a), .driver_info = USB_QUIRK_RESET_RESUME },
 
