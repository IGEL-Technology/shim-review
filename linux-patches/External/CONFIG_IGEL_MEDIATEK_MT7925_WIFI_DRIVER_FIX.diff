IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
	  AMD devices comes with Mediatek wifi chips which as stability issues.
	  This option provides additional stability fixes for the MediaTek MT7921
	  and MT7925 drivers.

	  Source: External
	  URL: https://github.com/zbowling/mt7925/tree/main/kernels/6.18
diff --git a/drivers/net/wireless/mediatek/mt76/mac80211.c b/drivers/net/wireless/mediatek/mt76/mac80211.c
index 76eb740428b8..9aed4a5a0d6a 100644
--- a/drivers/net/wireless/mediatek/mt76/mac80211.c
@@ -1713,6 +1713,18 @@ void mt76_wcid_cleanup(struct mt76_dev *dev, struct mt76_wcid *wcid)
 
 	idr_destroy(&wcid->pktid);
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	/* Remove from sta_poll_list to prevent list corruption after reset.
+	 * Without this, mt76_reset_device() reinitializes sta_poll_list but
+	 * leaves wcid->poll_list with stale pointers, causing list corruption
+	 * when mt76_wcid_add_poll() checks list_empty().
+	 */
+	spin_lock_bh(&dev->sta_poll_lock);
+	if (!list_empty(&wcid->poll_list))
+		list_del_init(&wcid->poll_list);
+	spin_unlock_bh(&dev->sta_poll_lock);
+#endif
+
 	spin_lock_bh(&phy->tx_lock);
 
 	if (!list_empty(&wcid->tx_list))
diff --git a/drivers/net/wireless/mediatek/mt76/mt76.h b/drivers/net/wireless/mediatek/mt76/mt76.h
index 125ac1eb2d54..47ace0129a1f 100644
--- a/drivers/net/wireless/mediatek/mt76/mt76.h
@@ -500,6 +500,9 @@ enum {
 	MT76_STATE_POWER_OFF,
 	MT76_STATE_SUSPEND,
 	MT76_STATE_ROC,
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	MT76_STATE_ROC_ABORT,
+#endif
 	MT76_STATE_PM,
 	MT76_STATE_WED_RESET,
 };
diff --git a/drivers/net/wireless/mediatek/mt76/mt7921/mac.c b/drivers/net/wireless/mediatek/mt76/mt7921/mac.c
index bce26389ab18..9a93ee8899bf 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7921/mac.c
@@ -693,9 +693,15 @@ void mt7921_mac_reset_work(struct work_struct *work)
 	clear_bit(MT76_RESET, &dev->mphy.state);
 	pm->suspended = false;
 	ieee80211_wake_queues(hw);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	mt792x_mutex_acquire(dev);
+#endif
 	ieee80211_iterate_active_interfaces(hw,
 					    IEEE80211_IFACE_ITER_RESUME_ALL,
 					    mt7921_vif_connect_iter, NULL);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	mt792x_mutex_release(dev);
+#endif
 	mt76_connac_power_save_sched(&dev->mt76.phy, pm);
 }
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt7921/main.c b/drivers/net/wireless/mediatek/mt76/mt7921/main.c
index ce11666a32db..d641281d4724 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7921/main.c
@@ -367,6 +367,20 @@ static void mt7921_roc_iter(void *priv, u8 *mac,
 	mt7921_mcu_abort_roc(phy, mvif, phy->roc_token_id);
 }
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+/* Async ROC abort - safe to call while holding mutex.
+ * Sets abort flag and schedules roc_work for cleanup.
+ */
+static void mt7921_roc_abort_async(struct mt792x_dev *dev)
+{
+	struct mt792x_phy *phy = &dev->phy;
+
+	set_bit(MT76_STATE_ROC_ABORT, &phy->mt76->state);
+	timer_delete(&phy->roc_timer);
+	ieee80211_queue_work(phy->mt76->hw, &phy->roc_work);
+}
+#endif
+
 void mt7921_roc_abort_sync(struct mt792x_dev *dev)
 {
 	struct mt792x_phy *phy = &dev->phy;
diff --git a/drivers/net/wireless/mediatek/mt76/mt7921/main.c b/drivers/net/wireless/mediatek/mt76/mt7921/main.c
index ce11666a32db..d641281d4724 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7921/main.c
@@ -390,6 +404,17 @@ void mt7921_roc_work(struct work_struct *work)
 	phy = (struct mt792x_phy *)container_of(work, struct mt792x_phy,
 						roc_work);
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	/* Check abort flag before acquiring mutex to prevent deadlock.
+	 * Only send expired callback if ROC was actually active.
+	 */
+	if (test_and_clear_bit(MT76_STATE_ROC_ABORT, &phy->mt76->state)) {
+		if (test_and_clear_bit(MT76_STATE_ROC, &phy->mt76->state))
+			ieee80211_remain_on_channel_expired(phy->mt76->hw);
+		return;
+	}
+#endif
+
 	mt792x_mutex_acquire(phy->dev);
 	if (!test_and_clear_bit(MT76_STATE_ROC, &phy->mt76->state)) {
 		mt792x_mutex_release(phy->dev);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7921/main.c b/drivers/net/wireless/mediatek/mt76/mt7921/main.c
index ce11666a32db..d641281d4724 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7921/main.c
@@ -890,7 +919,12 @@ void mt7921_mac_sta_remove(struct mt76_dev *mdev, struct ieee80211_vif *vif,
 	struct mt792x_dev *dev = container_of(mdev, struct mt792x_dev, mt76);
 	struct mt792x_sta *msta = (struct mt792x_sta *)sta->drv_priv;
 
+	/* Async abort - caller already holds mutex */
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	mt7921_roc_abort_async(dev);
+#else
 	mt7921_roc_abort_sync(dev);
+#endif
 	mt76_connac_free_pending_tx_skbs(&dev->pm, &msta->deflink.wcid);
 	mt76_connac_pm_wake(&dev->mphy, &dev->pm);
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/mac.c b/drivers/net/wireless/mediatek/mt76/mt7925/mac.c
index 0608f0c1fd2c..33ce4f525d96 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/mac.c
@@ -1276,6 +1289,14 @@ mt7925_vif_connect_iter(void *priv, u8 *mac,
 		bss_conf = mt792x_vif_to_bss_conf(vif, i);
 		mconf = mt792x_vif_to_link(mvif, i);
 
+		/* Skip links that don't have bss_conf set up yet in mac80211.
+		 * This can happen during HW reset when link state is inconsistent.
+		 */
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!bss_conf)
+			continue;
+#endif
+
 		mt76_connac_mcu_uni_add_dev(&dev->mphy, bss_conf, &mconf->mt76,
 					    &mvif->sta.deflink.wcid, true);
 		mt7925_mcu_set_tx(dev, bss_conf);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/mac.c b/drivers/net/wireless/mediatek/mt76/mt7925/mac.c
index 0608f0c1fd2c..33ce4f525d96 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/mac.c
@@ -1330,9 +1351,15 @@ void mt7925_mac_reset_work(struct work_struct *work)
 	dev->hw_full_reset = false;
 	pm->suspended = false;
 	ieee80211_wake_queues(hw);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	mt792x_mutex_acquire(dev);
+#endif
 	ieee80211_iterate_active_interfaces(hw,
 					    IEEE80211_IFACE_ITER_RESUME_ALL,
 					    mt7925_vif_connect_iter, NULL);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	mt792x_mutex_release(dev);
+#endif
 	mt76_connac_power_save_sched(&dev->mt76.phy, pm);
 
 	mt792x_mutex_acquire(dev);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -472,6 +545,19 @@ void mt7925_roc_work(struct work_struct *work)
 	phy = (struct mt792x_phy *)container_of(work, struct mt792x_phy,
 						roc_work);
 
+	/* Check abort flag BEFORE acquiring mutex to prevent deadlock.
+	 * If abort is requested while we're in the sta_remove path (which
+	 * holds the mutex), we must not try to acquire it or we'll deadlock.
+	 * Clear the flags and only notify mac80211 if ROC was actually active.
+	 */
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (test_and_clear_bit(MT76_STATE_ROC_ABORT, &phy->mt76->state)) {
+		if (test_and_clear_bit(MT76_STATE_ROC, &phy->mt76->state))
+			ieee80211_remain_on_channel_expired(phy->mt76->hw);
+		return;
+	}
+#endif
+
 	if (!test_and_clear_bit(MT76_STATE_ROC, &phy->mt76->state))
 		return;
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -499,14 +585,99 @@ static int mt7925_abort_roc(struct mt792x_phy *phy,
 	return err;
 }
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+/* ROC rate limiting constants - exponential backoff to prevent MCU overload
+ * when upper layers trigger rapid reconnection cycles (e.g., MLO auth failures).
+ * Max backoff ~1.6s, resets after 10s of no timeouts.
+ */
+#define MT7925_ROC_BACKOFF_BASE_MS	100
+#define MT7925_ROC_BACKOFF_MAX_MS	1600
+#define MT7925_ROC_TIMEOUT_RESET_MS	10000
+#define MT7925_ROC_TIMEOUT_WARN_THRESH	5
+
+/* Check if ROC should be throttled due to recent timeouts.
+ * Returns delay in jiffies if throttling, 0 if OK to proceed.
+ */
+static unsigned long mt7925_roc_throttle_check(struct mt792x_phy *phy)
+{
+	unsigned long now = jiffies;
+
+	/* Reset timeout counter if it's been a while since last timeout */
+	if (phy->roc_timeout_count &&
+	    time_after(now, phy->roc_last_timeout +
+		       msecs_to_jiffies(MT7925_ROC_TIMEOUT_RESET_MS))) {
+		phy->roc_timeout_count = 0;
+		phy->roc_backoff_until = 0;
+	}
+
+	/* Check if we're still in backoff period */
+	if (phy->roc_backoff_until && time_before(now, phy->roc_backoff_until))
+		return phy->roc_backoff_until - now;
+
+	return 0;
+}
+
+/* Record ROC timeout and calculate backoff period */
+static void mt7925_roc_record_timeout(struct mt792x_phy *phy)
+{
+	unsigned int backoff_ms;
+
+	phy->roc_last_timeout = jiffies;
+	phy->roc_timeout_count++;
+
+	/* Exponential backoff: 100ms, 200ms, 400ms, 800ms, 1600ms (capped) */
+	backoff_ms = MT7925_ROC_BACKOFF_BASE_MS <<
+		     min_t(u8, phy->roc_timeout_count - 1, 4);
+	if (backoff_ms > MT7925_ROC_BACKOFF_MAX_MS)
+		backoff_ms = MT7925_ROC_BACKOFF_MAX_MS;
+
+	phy->roc_backoff_until = jiffies + msecs_to_jiffies(backoff_ms);
+
+	/* Warn if we're seeing repeated timeouts - likely upper layer issue */
+	if (phy->roc_timeout_count == MT7925_ROC_TIMEOUT_WARN_THRESH)
+		dev_warn(phy->dev->mt76.dev,
+			 "mt7925: %u consecutive ROC timeouts, possible mac80211/wpa_supplicant issue (MLO key race?)\n",
+			 phy->roc_timeout_count);
+}
+
+/* Clear timeout tracking on successful ROC */
+static void mt7925_roc_clear_timeout(struct mt792x_phy *phy)
+{
+	phy->roc_timeout_count = 0;
+	phy->roc_backoff_until = 0;
+}
+#endif
+
 static int mt7925_set_roc(struct mt792x_phy *phy,
 			  struct mt792x_bss_conf *mconf,
 			  struct ieee80211_channel *chan,
 			  int duration,
 			  enum mt7925_roc_req type)
 {
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	unsigned long throttle;
+#endif
 	int err;
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	/* Check rate limiting - if in backoff period, wait or return busy */
+	throttle = mt7925_roc_throttle_check(phy);
+	if (throttle) {
+		/* For short backoffs, wait; for longer ones, return busy */
+		if (throttle < msecs_to_jiffies(200)) {
+			msleep(jiffies_to_msecs(throttle));
+		} else {
+			dev_dbg(phy->dev->mt76.dev,
+				"mt7925: ROC throttled, %u ms remaining\n",
+				jiffies_to_msecs(throttle));
+			return -EBUSY;
+		}
+	}
+
+	/* Clear stale abort flag from previous ROC */
+	clear_bit(MT76_STATE_ROC_ABORT, &phy->mt76->state);
+#endif
+
 	if (test_and_set_bit(MT76_STATE_ROC, &phy->mt76->state))
 		return -EBUSY;
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -522,7 +693,15 @@ static int mt7925_set_roc(struct mt792x_phy *phy,
 	if (!wait_event_timeout(phy->roc_wait, phy->roc_grant, 4 * HZ)) {
 		mt7925_mcu_abort_roc(phy, mconf, phy->roc_token_id);
 		clear_bit(MT76_STATE_ROC, &phy->mt76->state);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		mt7925_roc_record_timeout(phy);
+#endif
 		err = -ETIMEDOUT;
+	} else {
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		/* Successful ROC - reset timeout tracking */
+		mt7925_roc_clear_timeout(phy);
+#endif
 	}
 
 out:
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -533,8 +712,31 @@ static int mt7925_set_mlo_roc(struct mt792x_phy *phy,
 			      struct mt792x_bss_conf *mconf,
 			      u16 sel_links)
 {
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	unsigned long throttle;
+#endif
 	int err;
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	/* Check rate limiting - MLO ROC is especially prone to rapid-fire
+	 * during reconnection cycles after MLO authentication failures.
+	 */
+	throttle = mt7925_roc_throttle_check(phy);
+	if (throttle) {
+		if (throttle < msecs_to_jiffies(200)) {
+			msleep(jiffies_to_msecs(throttle));
+		} else {
+			dev_dbg(phy->dev->mt76.dev,
+				"mt7925: MLO ROC throttled, %u ms remaining\n",
+				jiffies_to_msecs(throttle));
+			return -EBUSY;
+		}
+	}
+
+	/* Clear stale abort flag from previous ROC */
+	clear_bit(MT76_STATE_ROC_ABORT, &phy->mt76->state);
+#endif
+
 	if (WARN_ON_ONCE(test_and_set_bit(MT76_STATE_ROC, &phy->mt76->state)))
 		return -EBUSY;
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -549,7 +751,14 @@ static int mt7925_set_mlo_roc(struct mt792x_phy *phy,
 	if (!wait_event_timeout(phy->roc_wait, phy->roc_grant, 4 * HZ)) {
 		mt7925_mcu_abort_roc(phy, mconf, phy->roc_token_id);
 		clear_bit(MT76_STATE_ROC, &phy->mt76->state);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		mt7925_roc_record_timeout(phy);
+#endif
 		err = -ETIMEDOUT;
+	} else {
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		mt7925_roc_clear_timeout(phy);
+#endif
 	}
 
 out:
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -566,6 +775,9 @@ static int mt7925_remain_on_channel(struct ieee80211_hw *hw,
 	struct mt792x_phy *phy = mt792x_hw_phy(hw);
 	int err;
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	cancel_work_sync(&phy->roc_work);
+#endif
 	mt792x_mutex_acquire(phy->dev);
 	err = mt7925_set_roc(phy, &mvif->bss_conf,
 			     chan, duration, MT7925_ROC_REQ_ROC);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -603,6 +815,19 @@ static int mt7925_set_link_key(struct ieee80211_hw *hw, enum set_key_cmd cmd,
 	link_sta = sta ? mt792x_sta_to_link_sta(vif, sta, link_id) : NULL;
 	mconf = mt792x_vif_to_link(mvif, link_id);
 	mlink = mt792x_sta_to_link(msta, link_id);
+
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (!link_conf || !mconf || !mlink) {
+		/* During MLO roaming, link state may be torn down before
+		 * mac80211 requests key removal. If removing a key and
+		 * the link is already gone, consider it successfully removed.
+		 */
+		if (cmd != SET_KEY)
+			return 0;
+		return -EINVAL;
+	}
+#endif
+
 	wcid = &mlink->wcid;
 	wcid_keyidx = &wcid->hw_key_idx;
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -632,8 +857,15 @@ static int mt7925_set_link_key(struct ieee80211_hw *hw, enum set_key_cmd cmd,
 		struct mt792x_phy *phy = mt792x_hw_phy(hw);
 
 		mconf->mt76.cipher = mt7925_mcu_get_cipher(key->cipher);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		err = mt7925_mcu_add_bss_info(phy, mconf->mt76.ctx, link_conf,
+						link_sta, true);
+		if (err)
+			goto out;
+#else
 		mt7925_mcu_add_bss_info(phy, mconf->mt76.ctx, link_conf,
 					link_sta, true);
+#endif
 	}
 
 	if (cmd == SET_KEY)
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -750,9 +982,15 @@ void mt7925_set_runtime_pm(struct mt792x_dev *dev)
 	bool monitor = !!(hw->conf.flags & IEEE80211_CONF_MONITOR);
 
 	pm->enable = pm->enable_user && !monitor;
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	mt792x_mutex_acquire(dev);
+#endif
 	ieee80211_iterate_active_interfaces(hw,
 					    IEEE80211_IFACE_ITER_RESUME_ALL,
 					    mt7925_pm_interface_iter, dev);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	mt792x_mutex_release(dev);
+#endif
 	pm->ds_enable = pm->ds_enable_user && !monitor;
 	mt7925_mcu_set_deep_sleep(dev, pm->ds_enable);
 }
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -855,12 +1093,22 @@ static int mt7925_mac_link_sta_add(struct mt76_dev *mdev,
 
 	msta = (struct mt792x_sta *)link_sta->sta->drv_priv;
 	mlink = mt792x_sta_to_link(msta, link_id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (!mlink)
+		return -EINVAL;
+
+	mconf = mt792x_vif_to_link(mvif, link_id);
+	if (!mconf)
+		return -EINVAL;
+#endif
 
 	idx = mt76_wcid_alloc(dev->mt76.wcid_mask, MT792x_WTBL_STA - 1);
 	if (idx < 0)
 		return -ENOSPC;
 
+#ifndef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
 	mconf = mt792x_vif_to_link(mvif, link_id);
+#endif
 	mt76_wcid_init(&mlink->wcid, 0);
 	mlink->wcid.sta = 1;
 	mlink->wcid.idx = idx;
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -873,28 +1121,55 @@ static int mt7925_mac_link_sta_add(struct mt76_dev *mdev,
 	wcid = &mlink->wcid;
 	ewma_signal_init(&wcid->rssi);
 	rcu_assign_pointer(dev->mt76.wcid[wcid->idx], wcid);
+#ifndef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
 	mt76_wcid_init(wcid, 0);
+#endif
 	ewma_avg_signal_init(&mlink->avg_ack_signal);
 	memset(mlink->airtime_ac, 0,
 	       sizeof(msta->deflink.airtime_ac));
 
 	ret = mt76_connac_pm_wake(&dev->mphy, &dev->pm);
 	if (ret)
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		goto err_wcid;
+#else
 		return ret;
+#endif
 
 	mt7925_mac_wtbl_update(dev, idx,
 			       MT_WTBL_UPDATE_ADM_COUNT_CLEAR);
 
 	link_conf = mt792x_vif_to_bss_conf(vif, link_id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (!link_conf) {
+		ret = -EINVAL;
+		goto err_wcid;
+	}
+#endif
 
 	/* should update bss info before STA add */
 	if (vif->type == NL80211_IFTYPE_STATION && !link_sta->sta->tdls) {
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (ieee80211_vif_is_mld(vif))
+			ret = mt7925_mcu_add_bss_info(&dev->phy, mconf->mt76.ctx,
+						      link_conf, link_sta,
+						      link_sta != mlink->pri_link);
+		else
+			ret = mt7925_mcu_add_bss_info(&dev->phy, mconf->mt76.ctx,
+						      link_conf, link_sta, false);
+#else
 		if (ieee80211_vif_is_mld(vif))
 			mt7925_mcu_add_bss_info(&dev->phy, mconf->mt76.ctx,
 						link_conf, link_sta, link_sta != mlink->pri_link);
 		else
 			mt7925_mcu_add_bss_info(&dev->phy, mconf->mt76.ctx,
 						link_conf, link_sta, false);
+#endif
+
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (ret)
+			goto err_wcid;
+#endif
 	}
 
 	if (ieee80211_vif_is_mld(vif) &&
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -902,28 +1177,51 @@ static int mt7925_mac_link_sta_add(struct mt76_dev *mdev,
 		ret = mt7925_mcu_sta_update(dev, link_sta, vif, true,
 					    MT76_STA_INFO_STATE_NONE);
 		if (ret)
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+			goto err_wcid;
+#else
 			return ret;
+#endif
 	} else if (ieee80211_vif_is_mld(vif) &&
 		   link_sta != mlink->pri_link) {
 		ret = mt7925_mcu_sta_update(dev, mlink->pri_link, vif,
 					    true, MT76_STA_INFO_STATE_ASSOC);
 		if (ret)
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+			goto err_wcid;
+#else
 			return ret;
+#endif
 
 		ret = mt7925_mcu_sta_update(dev, link_sta, vif, true,
 					    MT76_STA_INFO_STATE_ASSOC);
 		if (ret)
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+			goto err_wcid;
+#else
 			return ret;
+#endif
 	} else {
 		ret = mt7925_mcu_sta_update(dev, link_sta, vif, true,
 					    MT76_STA_INFO_STATE_NONE);
 		if (ret)
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+			goto err_wcid;
+#else
 			return ret;
+#endif
 	}
 
 	mt76_connac_power_save_sched(&dev->mphy, &dev->pm);
 
 	return 0;
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+err_wcid:
+	rcu_assign_pointer(dev->mt76.wcid[idx], NULL);
+	mt76_wcid_mask_clear(dev->mt76.wcid_mask, idx);
+	mt76_connac_power_save_sched(&dev->mphy, &dev->pm);
+	return ret;
+#endif
 }
 
 static int
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -992,18 +1290,43 @@ mt7925_mac_set_links(struct mt76_dev *mdev, struct ieee80211_vif *vif)
 {
 	struct mt792x_dev *dev = container_of(mdev, struct mt792x_dev, mt76);
 	struct mt792x_vif *mvif = (struct mt792x_vif *)vif->drv_priv;
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	struct ieee80211_bss_conf *link_conf;
+	struct cfg80211_chan_def *chandef;
+	enum nl80211_band band, secondary_band;
+	u16 sel_links;
+	u8 secondary_link_id;
+#else
 	struct ieee80211_bss_conf *link_conf =
 		mt792x_vif_to_bss_conf(vif, mvif->deflink_id);
 	struct cfg80211_chan_def *chandef = &link_conf->chanreq.oper;
 	enum nl80211_band band = chandef->chan->band, secondary_band;
+#endif
+
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	link_conf = mt792x_vif_to_bss_conf(vif, mvif->deflink_id);
+	if (!link_conf)
+		return;
+
+	chandef = &link_conf->chanreq.oper;
+	band = chandef->chan->band;
 
+	sel_links = mt76_select_links(vif, 2);
+	secondary_link_id = __ffs(~BIT(mvif->deflink_id) & sel_links);
+#else
 	u16 sel_links = mt76_select_links(vif, 2);
 	u8 secondary_link_id = __ffs(~BIT(mvif->deflink_id) & sel_links);
+#endif
 
 	if (!ieee80211_vif_is_mld(vif) || hweight16(sel_links) < 2)
 		return;
 
 	link_conf = mt792x_vif_to_bss_conf(vif, secondary_link_id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (!link_conf)
+		return;
+#endif
+
 	secondary_band = link_conf->chanreq.oper.chan->band;
 
 	if (band == NL80211_BAND_2GHZ ||
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1031,6 +1354,10 @@ static void mt7925_mac_link_sta_assoc(struct mt76_dev *mdev,
 
 	msta = (struct mt792x_sta *)link_sta->sta->drv_priv;
 	mlink = mt792x_sta_to_link(msta, link_sta->link_id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (!mlink)
+		return;
+#endif
 
 	mt792x_mutex_acquire(dev);
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1040,12 +1367,22 @@ static void mt7925_mac_link_sta_assoc(struct mt76_dev *mdev,
 		link_conf = mt792x_vif_to_bss_conf(vif, vif->bss_conf.link_id);
 	}
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (link_conf && vif->type == NL80211_IFTYPE_STATION && !link_sta->sta->tdls) {
+#else
 	if (vif->type == NL80211_IFTYPE_STATION && !link_sta->sta->tdls) {
+#endif
 		struct mt792x_bss_conf *mconf;
 
 		mconf = mt792x_link_conf_to_mconf(link_conf);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (mconf)
+			mt7925_mcu_add_bss_info(&dev->phy, mconf->mt76.ctx,
+						link_conf, link_sta, true);
+#else
 		mt7925_mcu_add_bss_info(&dev->phy, mconf->mt76.ctx,
 					link_conf, link_sta, true);
+#endif
 	}
 
 	ewma_avg_signal_init(&mlink->avg_ack_signal);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1092,7 +1429,14 @@ static void mt7925_mac_link_sta_remove(struct mt76_dev *mdev,
 
 	msta = (struct mt792x_sta *)link_sta->sta->drv_priv;
 	mlink = mt792x_sta_to_link(msta, link_id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (!mlink)
+		return;
+#endif
 
+	/* With Sean's fix, roc_abort_sync uses cancel_work() instead of
+	 * cancel_work_sync(), so it's safe to call even with mutex held.
+	 */
 	mt7925_roc_abort_sync(dev);
 
 	mt76_connac_free_pending_tx_skbs(&dev->pm, &mlink->wcid);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1105,18 +1449,27 @@ static void mt7925_mac_link_sta_remove(struct mt76_dev *mdev,
 
 	link_conf = mt792x_vif_to_bss_conf(vif, link_id);
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (link_conf && vif->type == NL80211_IFTYPE_STATION && !link_sta->sta->tdls) {
+#else
 	if (vif->type == NL80211_IFTYPE_STATION && !link_sta->sta->tdls) {
+#endif
 		struct mt792x_bss_conf *mconf;
 
 		mconf = mt792x_link_conf_to_mconf(link_conf);
-
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!mconf)
+			goto out;
+#endif
 		if (ieee80211_vif_is_mld(vif))
 			mt792x_mac_link_bss_remove(dev, mconf, mlink);
 		else
 			mt7925_mcu_add_bss_info(&dev->phy, mconf->mt76.ctx, link_conf,
 						link_sta, false);
 	}
-
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+out:
+#endif
 	spin_lock_bh(&mdev->sta_poll_lock);
 	if (!list_empty(&mlink->wcid.poll_list))
 		list_del_init(&mlink->wcid.poll_list);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1254,22 +1607,38 @@ mt7925_ampdu_action(struct ieee80211_hw *hw, struct ieee80211_vif *vif,
 	case IEEE80211_AMPDU_RX_START:
 		mt76_rx_aggr_start(&dev->mt76, &msta->deflink.wcid, tid, ssn,
 				   params->buf_size);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		ret = mt7925_mcu_uni_rx_ba(dev, params, true);
+#else
 		mt7925_mcu_uni_rx_ba(dev, params, true);
+#endif
 		break;
 	case IEEE80211_AMPDU_RX_STOP:
 		mt76_rx_aggr_stop(&dev->mt76, &msta->deflink.wcid, tid);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		ret = mt7925_mcu_uni_rx_ba(dev, params, false);
+#else
 		mt7925_mcu_uni_rx_ba(dev, params, false);
+#endif
 		break;
 	case IEEE80211_AMPDU_TX_OPERATIONAL:
 		mtxq->aggr = true;
 		mtxq->send_bar = false;
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		ret = mt7925_mcu_uni_tx_ba(dev, params, true);
+#else
 		mt7925_mcu_uni_tx_ba(dev, params, true);
+#endif
 		break;
 	case IEEE80211_AMPDU_TX_STOP_FLUSH:
 	case IEEE80211_AMPDU_TX_STOP_FLUSH_CONT:
 		mtxq->aggr = false;
 		clear_bit(tid, &msta->deflink.wcid.ampdu_state);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		ret = mt7925_mcu_uni_tx_ba(dev, params, false);
+#else
 		mt7925_mcu_uni_tx_ba(dev, params, false);
+#endif
 		break;
 	case IEEE80211_AMPDU_TX_START:
 		set_bit(tid, &msta->deflink.wcid.ampdu_state);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1300,12 +1674,20 @@ mt7925_mlo_pm_iter(void *priv, u8 *mac, struct ieee80211_vif *vif)
 	if (mvif->mlo_pm_state != MT792x_MLO_CHANGED_PS)
 		return;
 
+#ifndef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
 	mt792x_mutex_acquire(dev);
+#endif
 	for_each_set_bit(i, &valid, IEEE80211_MLD_MAX_NUM_LINKS) {
 		bss_conf = mt792x_vif_to_bss_conf(vif, i);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!bss_conf)
+			continue;
+#endif
 		mt7925_mcu_uni_bss_ps(dev, bss_conf);
 	}
+#ifndef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
 	mt792x_mutex_release(dev);
+#endif
 }
 
 void mt7925_mlo_pm_work(struct work_struct *work)
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1314,9 +1696,15 @@ void mt7925_mlo_pm_work(struct work_struct *work)
 					      mlo_pm_work.work);
 	struct ieee80211_hw *hw = mt76_hw(dev);
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	mt792x_mutex_acquire(dev);
+#endif
 	ieee80211_iterate_active_interfaces(hw,
 					    IEEE80211_IFACE_ITER_RESUME_ALL,
 					    mt7925_mlo_pm_iter, dev);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	mt792x_mutex_release(dev);
+#endif
 }
 
 static bool is_valid_alpha2(const char *alpha2)
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1501,6 +1889,10 @@ static int mt7925_suspend(struct ieee80211_hw *hw,
 	cancel_delayed_work_sync(&dev->pm.ps_work);
 	mt76_connac_free_pending_tx_skbs(&dev->pm, NULL);
 
+	/* Cancel ROC before quiescing starts */
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	mt7925_roc_abort_sync(dev);
+#endif
 	mt792x_mutex_acquire(dev);
 
 	clear_bit(MT76_STATE_RUNNING, &phy->mt76->state);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1652,6 +2044,10 @@ static void mt7925_ipv6_addr_change(struct ieee80211_hw *hw,
 
 	for_each_set_bit(i, &valid, IEEE80211_MLD_MAX_NUM_LINKS) {
 		bss_conf = mt792x_vif_to_bss_conf(vif, i);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!bss_conf)
+			continue;
+#endif
 		__mt7925_ipv6_addr_change(hw, bss_conf, idev);
 	}
 }
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1713,6 +2109,11 @@ mt7925_conf_tx(struct ieee80211_hw *hw, struct ieee80211_vif *vif,
 		    [IEEE80211_AC_BK] = 1,
 	};
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (!mconf)
+		return -EINVAL;
+#endif
+
 	/* firmware uses access class index */
 	mconf->queue_params[mq_to_aci[queue]] = *params;
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1842,6 +2243,10 @@ static void mt7925_mgd_prepare_tx(struct ieee80211_hw *hw,
 	u16 duration = info->duration ? info->duration :
 		       jiffies_to_msecs(HZ);
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	cancel_work_sync(&mvif->phy->roc_work);
+#endif
+
 	mt792x_mutex_acquire(dev);
 	mt7925_set_roc(mvif->phy, &mvif->bss_conf,
 		       mvif->bss_conf.mt76.ctx->def.chan, duration,
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1883,6 +2288,10 @@ static void mt7925_vif_cfg_changed(struct ieee80211_hw *hw,
 	if (changed & BSS_CHANGED_ARP_FILTER) {
 		for_each_set_bit(i, &valid, IEEE80211_MLD_MAX_NUM_LINKS) {
 			bss_conf = mt792x_vif_to_bss_conf(vif, i);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+			if (!bss_conf)
+				continue;
+#endif
 			mt7925_mcu_update_arp_filter(&dev->mt76, bss_conf);
 		}
 	}
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1898,6 +2307,10 @@ static void mt7925_vif_cfg_changed(struct ieee80211_hw *hw,
 			} else if (mvif->mlo_pm_state == MT792x_MLO_CHANGED_PS) {
 				for_each_set_bit(i, &valid, IEEE80211_MLD_MAX_NUM_LINKS) {
 					bss_conf = mt792x_vif_to_bss_conf(vif, i);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+					if (!bss_conf)
+						continue;
+#endif
 					mt7925_mcu_uni_bss_ps(dev, bss_conf);
 				}
 			}
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1918,6 +2331,10 @@ static void mt7925_link_info_changed(struct ieee80211_hw *hw,
 	struct mt792x_bss_conf *mconf;
 
 	mconf = mt792x_vif_to_link(mvif, info->link_id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (!mconf)
+		return;
+#endif
 
 	mt792x_mutex_acquire(dev);
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -1984,6 +2409,9 @@ mt7925_change_vif_links(struct ieee80211_hw *hw, struct ieee80211_vif *vif,
 	if (old_links == new_links)
 		return 0;
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	cancel_work_sync(&phy->roc_work);
+#endif
 	mt792x_mutex_acquire(dev);
 
 	for_each_set_bit(link_id, &rem, IEEE80211_MLD_MAX_NUM_LINKS) {
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -2036,6 +2464,12 @@ mt7925_change_vif_links(struct ieee80211_hw *hw, struct ieee80211_vif *vif,
 		mconf = mconfs[link_id];
 		mlink = mlinks[link_id];
 		link_conf = mt792x_vif_to_bss_conf(vif, link_id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!link_conf) {
+			err = -EINVAL;
+			goto free;
+		}
+#endif
 
 		rcu_assign_pointer(mvif->link_conf[link_id], mconf);
 		rcu_assign_pointer(mvif->sta.link[link_id], mlink);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -2109,24 +2550,60 @@ static int mt7925_assign_vif_chanctx(struct ieee80211_hw *hw,
 {
 	struct mt792x_chanctx *mctx = (struct mt792x_chanctx *)ctx->drv_priv;
 	struct mt792x_vif *mvif = (struct mt792x_vif *)vif->drv_priv;
+#ifdef CONFIG_IGEL_ADD_SUPPORT_FOR_MEDIATEK_MT7927
+	struct mt792x_link_sta *mlink;
+#endif
 	struct mt792x_dev *dev = mt792x_hw_dev(hw);
 	struct ieee80211_bss_conf *pri_link_conf;
 	struct mt792x_bss_conf *mconf;
+#ifdef CONFIG_IGEL_ADD_SUPPORT_FOR_MEDIATEK_MT7927
+	u8 band_idx;
+	u8 old_band;
+#endif
 
 	mutex_lock(&dev->mt76.mutex);
 
 	if (ieee80211_vif_is_mld(vif)) {
 		mconf = mt792x_vif_to_link(mvif, link_conf->link_id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!mconf) {
+			mutex_unlock(&dev->mt76.mutex);
+			return -EINVAL;
+		}
+#endif
+
 		pri_link_conf = mt792x_vif_to_bss_conf(vif, mvif->deflink_id);
 
-		if (vif->type == NL80211_IFTYPE_STATION &&
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (pri_link_conf && vif->type == NL80211_IFTYPE_STATION &&
 		    mconf == &mvif->bss_conf)
+#else
+		if (vif->type == NL80211_IFTYPE_STATION &&
+			mconf == &mvif->bss_conf)
+#endif
 			mt7925_mcu_add_bss_info(&dev->phy, NULL, pri_link_conf,
 						NULL, true);
 	} else {
 		mconf = &mvif->bss_conf;
 	}
 
+#ifdef CONFIG_IGEL_ADD_SUPPORT_FOR_MEDIATEK_MT7927
+	old_band = mconf->mt76.band_idx;
+	if (ctx->def.chan) {
+		band_idx = mt7925_band_idx(&dev->mt76, ctx->def.chan->band);
+		mconf->mt76.band_idx = band_idx;
+		mlink = mt792x_sta_to_link(&mvif->sta, mconf->link_id);
+		if (mlink)
+			mlink->wcid.phy_idx = band_idx;
+
+		if (old_band != band_idx && vif->type == NL80211_IFTYPE_STATION &&
+		    mlink)
+			mt76_connac_mcu_uni_add_dev(&dev->mphy, link_conf,
+						    &mconf->mt76, &mlink->wcid,
+						    true);
+	}
+#endif
+
 	mconf->mt76.ctx = ctx;
 	mctx->bss_conf = mconf;
 	mutex_unlock(&dev->mt76.mutex);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/main.c b/drivers/net/wireless/mediatek/mt76/mt7925/main.c
index 00126f563dfd..1918c264ee0e 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/main.c
@@ -2148,6 +2625,12 @@ static void mt7925_unassign_vif_chanctx(struct ieee80211_hw *hw,
 
 	if (ieee80211_vif_is_mld(vif)) {
 		mconf = mt792x_vif_to_link(mvif, link_conf->link_id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!mconf) {
+			mutex_unlock(&dev->mt76.mutex);
+			return;
+		}
+#endif
 
 		if (vif->type == NL80211_IFTYPE_STATION &&
 		    mconf == &mvif->bss_conf)
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c b/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
index d887aa9a3dff..bb4a22b600c6 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
@@ -1089,6 +1136,10 @@ mt7925_mcu_sta_hdr_trans_tlv(struct sk_buff *skb,
 		struct mt792x_link_sta *mlink;
 
 		mlink = mt792x_sta_to_link(msta, link_sta->link_id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!mlink)
+			return;
+#endif
 		wcid = &mlink->wcid;
 	} else {
 		wcid = &mvif->sta.deflink.wcid;
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c b/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
index d887aa9a3dff..bb4a22b600c6 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
@@ -1122,6 +1173,11 @@ int mt7925_mcu_wtbl_update_hdr_trans(struct mt792x_dev *dev,
 	link_sta = mt792x_sta_to_link_sta(vif, sta, link_id);
 	mconf = mt792x_vif_to_link(mvif, link_id);
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (!mlink || !mconf)
+		return -EINVAL;
+#endif
+
 	skb = __mt76_connac_mcu_alloc_sta_req(&dev->mt76, &mconf->mt76,
 					      &mlink->wcid,
 					      MT7925_STA_UPDATE_MAX_SIZE);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c b/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
index d887aa9a3dff..bb4a22b600c6 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
@@ -1341,15 +1397,35 @@ int mt7925_mcu_set_mlo_roc(struct mt792x_phy *phy, struct mt792x_bss_conf *mconf
 	for (i = 0; i < ARRAY_SIZE(links); i++) {
 		links[i].id = i ? __ffs(~BIT(mconf->link_id) & sel_links) :
 				 mconf->link_id;
+
 		link_conf = mt792x_vif_to_bss_conf(vif, links[i].id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!link_conf)
+			return -ENOLINK;
+#else
 		if (WARN_ON_ONCE(!link_conf))
 			return -EPERM;
+#endif
 
 		links[i].chan = link_conf->chanreq.oper.chan;
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!links[i].chan)
+			/* Channel not configured yet - this can happen during
+			 * MLO AP setup when links are being added sequentially.
+			 * Return -ENOLINK to indicate link not ready.
+			 */
+			return -ENOLINK;
+#else
 		if (WARN_ON_ONCE(!links[i].chan))
 			return -EPERM;
+#endif
 
 		links[i].mconf = mt792x_vif_to_link(mvif, links[i].id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!links[i].mconf)
+			return -ENOLINK;
+#endif
+
 		links[i].tag = links[i].id == mconf->link_id ?
 			       UNI_ROC_ACQUIRE : UNI_ROC_SUB_LINK;
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c b/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
index d887aa9a3dff..bb4a22b600c6 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
@@ -1364,8 +1440,13 @@ int mt7925_mcu_set_mlo_roc(struct mt792x_phy *phy, struct mt792x_bss_conf *mconf
 		type = MT7925_ROC_REQ_JOIN;
 
 	for (i = 0; i < ARRAY_SIZE(links) && i < hweight16(vif->active_links); i++) {
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!links[i].mconf || !links[i].chan)
+			return -ENOLINK;
+#else
 		if (WARN_ON_ONCE(!links[i].mconf || !links[i].chan))
 			continue;
+#endif
 
 		chan = links[i].chan;
 		center_ch = ieee80211_frequency_to_channel(chan->center_freq);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c b/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
index d887aa9a3dff..bb4a22b600c6 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
@@ -1532,6 +1635,10 @@ int mt7925_mcu_uni_bss_ps(struct mt792x_dev *dev,
 		},
 	};
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	lockdep_assert_held(&dev->mt76.mutex);
+#endif
+
 	if (link_conf->vif->type != NL80211_IFTYPE_STATION)
 		return -EOPNOTSUPP;
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c b/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
index d887aa9a3dff..bb4a22b600c6 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
@@ -1746,6 +1926,10 @@ mt7925_mcu_sta_amsdu_tlv(struct sk_buff *skb,
 	amsdu->amsdu_en = true;
 
 	mlink = mt792x_sta_to_link(msta, link_sta->link_id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (!mlink)
+		return;
+#endif
 	mlink->wcid.amsdu = true;
 
 	switch (link_sta->agg.max_amsdu_len) {
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c b/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
index d887aa9a3dff..bb4a22b600c6 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
@@ -1778,6 +1962,12 @@ mt7925_mcu_sta_phy_tlv(struct sk_buff *skb,
 
 	link_conf = mt792x_vif_to_bss_conf(vif, link_sta->link_id);
 	mconf = mt792x_vif_to_link(mvif, link_sta->link_id);
+
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (!link_conf || !mconf)
+		return;
+#endif
+
 	chandef = mconf->mt76.ctx ? &mconf->mt76.ctx->def :
 				    &link_conf->chanreq.oper;
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c b/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
index d887aa9a3dff..bb4a22b600c6 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
@@ -1856,6 +2046,12 @@ mt7925_mcu_sta_rate_ctrl_tlv(struct sk_buff *skb,
 
 	link_conf = mt792x_vif_to_bss_conf(vif, link_sta->link_id);
 	mconf = mt792x_vif_to_link(mvif, link_sta->link_id);
+
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	if (!link_conf || !mconf)
+		return;
+#endif
+
 	chandef = mconf->mt76.ctx ? &mconf->mt76.ctx->def :
 				    &link_conf->chanreq.oper;
 	band = chandef->chan->band;
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c b/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
index d887aa9a3dff..bb4a22b600c6 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
@@ -1940,6 +2136,11 @@ mt7925_mcu_sta_mld_tlv(struct sk_buff *skb,
 
 		mconf = mt792x_vif_to_link(mvif, i);
 		mlink = mt792x_sta_to_link(msta, i);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!mconf || !mlink)
+			continue;
+#endif
+
 		mld->link[cnt].wlan_id = cpu_to_le16(mlink->wcid.idx);
 		mld->link[cnt++].bss_idx = mconf->mt76.idx;
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c b/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
index d887aa9a3dff..bb4a22b600c6 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
@@ -2032,13 +2240,23 @@ int mt7925_mcu_sta_update(struct mt792x_dev *dev,
 		.rcpi = to_rcpi(rssi),
 	};
 	struct mt792x_sta *msta;
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	struct mt792x_link_sta *mlink = NULL;
+
+	lockdep_assert_held(&dev->mt76.mutex);
+#else
 	struct mt792x_link_sta *mlink;
+#endif
 
 	if (link_sta) {
 		msta = (struct mt792x_sta *)link_sta->sta->drv_priv;
 		mlink = mt792x_sta_to_link(msta, link_sta->link_id);
 	}
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	info.wcid = (link_sta && mlink) ? &mlink->wcid : &mvif->sta.deflink.wcid;
+#else
 	info.wcid = link_sta ? &mlink->wcid : &mvif->sta.deflink.wcid;
+#endif
 	info.newly = state != MT76_STA_INFO_STATE_ASSOC;
 
 	return mt7925_mcu_sta_cmd(&dev->mphy, &info);
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c b/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
index d887aa9a3dff..bb4a22b600c6 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/mcu.c
@@ -2840,6 +3068,10 @@ int mt7925_mcu_add_bss_info(struct mt792x_phy *phy,
 	struct mt792x_link_sta *mlink_bc;
 	struct sk_buff *skb;
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	lockdep_assert_held(&dev->mt76.mutex);
+#endif
+
 	skb = __mt7925_mcu_alloc_bss_req(&dev->mt76, &mconf->mt76,
 					 MT7925_BSS_UPDATE_MAX_SIZE);
 	if (IS_ERR(skb))
diff --git a/drivers/net/wireless/mediatek/mt76/mt7925/pci.c b/drivers/net/wireless/mediatek/mt76/mt7925/pci.c
index 8eb1fe1082d1..15ab35e1b51a 100644
--- a/drivers/net/wireless/mediatek/mt76/mt7925/pci.c
@@ -581,10 +877,16 @@ static int _mt7925_pci_resume(struct device *device, bool restore)
 	}
 
 	/* restore previous ds setting */
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	mt792x_mutex_acquire(dev);
+#endif
 	if (!pm->ds_enable)
 		mt7925_mcu_set_deep_sleep(dev, false);
 
 	mt7925_regd_update(dev);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	mt792x_mutex_release(dev);
+#endif
 failed:
 	pm->suspended = false;
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt792x.h b/drivers/net/wireless/mediatek/mt76/mt792x.h
index f2c8b9e4aa0f..78be2cbec4c7 100644
--- a/drivers/net/wireless/mediatek/mt76/mt792x.h
@@ -185,6 +201,15 @@ struct mt792x_phy {
 	wait_queue_head_t roc_wait;
 	u8 roc_token_id;
 	bool roc_grant;
+
+	/* ROC rate limiting to prevent MCU overload during rapid reconnection
+	 * cycles (e.g., MLO authentication failures causing repeated ROC).
+	 */
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	u8 roc_timeout_count;		/* consecutive ROC timeouts */
+	unsigned long roc_last_timeout;	/* jiffies of last timeout */
+	unsigned long roc_backoff_until;/* don't issue ROC until this time */
+#endif
 };
 
 struct mt792x_irq_map {
diff --git a/drivers/net/wireless/mediatek/mt76/mt792x_core.c b/drivers/net/wireless/mediatek/mt76/mt792x_core.c
index 9cad572c34a3..1fbeb57d478a 100644
--- a/drivers/net/wireless/mediatek/mt76/mt792x_core.c
@@ -95,6 +95,10 @@ void mt792x_tx(struct ieee80211_hw *hw, struct ieee80211_tx_control *control,
 				       IEEE80211_TX_CTRL_MLO_LINK);
 		sta = (struct mt792x_sta *)control->sta->drv_priv;
 		mlink = mt792x_sta_to_link(sta, link_id);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!mlink)
+			goto free_skb;
+#endif
 		wcid = &mlink->wcid;
 	}
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt792x_core.c b/drivers/net/wireless/mediatek/mt76/mt792x_core.c
index 9cad572c34a3..1fbeb57d478a 100644
--- a/drivers/net/wireless/mediatek/mt76/mt792x_core.c
@@ -113,9 +117,17 @@ void mt792x_tx(struct ieee80211_hw *hw, struct ieee80211_tx_control *control,
 		link_id = wcid->link_id;
 		rcu_read_lock();
 		conf = rcu_dereference(vif->link_conf[link_id]);
+#ifndef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
 		memcpy(hdr->addr2, conf->addr, ETH_ALEN);
-
+#endif
 		link_sta = rcu_dereference(control->sta->link[link_id]);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+		if (!conf || !link_sta) {
+			rcu_read_unlock();
+			goto free_skb;
+		}
+		memcpy(hdr->addr2, conf->addr, ETH_ALEN);
+#endif
 		memcpy(hdr->addr1, link_sta->addr, ETH_ALEN);
 
 		if (vif->type == NL80211_IFTYPE_STATION)
diff --git a/drivers/net/wireless/mediatek/mt76/mt792x_core.c b/drivers/net/wireless/mediatek/mt76/mt792x_core.c
index 9cad572c34a3..1fbeb57d478a 100644
--- a/drivers/net/wireless/mediatek/mt76/mt792x_core.c
@@ -136,6 +148,12 @@ void mt792x_tx(struct ieee80211_hw *hw, struct ieee80211_tx_control *control,
 	}
 
 	mt76_connac_pm_queue_skb(hw, &dev->pm, wcid, skb);
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	return;
+
+free_skb:
+	ieee80211_free_txskb(hw, skb);
+#endif
 }
 EXPORT_SYMBOL_GPL(mt792x_tx);
 
diff --git a/drivers/net/wireless/mediatek/mt76/mt792x_core.c b/drivers/net/wireless/mediatek/mt76/mt792x_core.c
index 9cad572c34a3..1fbeb57d478a 100644
--- a/drivers/net/wireless/mediatek/mt76/mt792x_core.c
@@ -927,6 +953,27 @@ int mt792x_load_firmware(struct mt792x_dev *dev)
 {
 	int ret;
 
+#ifdef CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX
+	/* Release semaphore if taken by previous failed load attempt.
+	 * This prevents "Failed to get patch semaphore" errors when
+	 * recovering from firmware crashes or suspend/resume failures.
+	 */
+	ret = mt76_connac_mcu_patch_sem_ctrl(&dev->mt76, false);
+	if (ret < 0)
+		dev_dbg(dev->mt76.dev, "Semaphore release returned %d (may be expected)\n", ret);
+#endif /* CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX */
+
+#if defined(CONFIG_IGEL_MEDIATEK_MT7925_WIFI_DRIVER_FIX) || defined(CONFIG_IGEL_ADD_SUPPORT_FOR_MEDIATEK_MT7902)
+	/* Always restart MCU to ensure clean state before loading firmware */
+	mt76_connac_mcu_restart(&dev->mt76);
+
+	/* Wait for MCU to be ready after restart */
+	if (!mt76_poll_msec(dev, MT_CONN_ON_MISC, MT_TOP_MISC_FW_STATE,
+			    MT_TOP_MISC2_FW_PWR_ON, 1000))
+		dev_warn(dev->mt76.dev,
+			 "MCU is not ready for firmware download\n");
+#endif
+
 	ret = mt76_connac2_load_patch(&dev->mt76, mt792x_patch_name(dev));
 	if (ret)
 		return ret;
